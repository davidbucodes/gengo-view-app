import styled from "styled-components";
import { colors, fontSizes } from "../../../../../../theme";

export namespace Styles {
  export const SelectRadicals = styled.div`
    display: flex;
    flex-grow: 1;
  `;
}
